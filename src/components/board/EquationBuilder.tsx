// DEPRECATED — all game logic lives in index.tsx
// This file is kept as a no-op to prevent import errors from other src/ files
import React from 'react'
import { View } from 'react-native'
export default function EquationBuilder() { return null; }
